# PostgreSQL Database URL
# For production, use a service like Supabase, Neon, or Railway
# Format: postgresql://user:password@host:port/database?sslmode=require
POSTGRES_URL=

# JWT Secret for authentication
# Generate a secure random string (at least 32 characters)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Node Environment
NODE_ENV=production

# Port (optional, Vercel sets this automatically)
PORT=3000

# Optional: Payment Processing (for future implementation)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Optional: Email Service (for future implementation)
SENDGRID_API_KEY=
EMAIL_FROM=noreply@yourdomain.com

# Optional: Analytics (for future implementation)
GA_MEASUREMENT_ID=
